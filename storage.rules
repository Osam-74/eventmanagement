rules_version = '2';

// All Storage access is server-only (Admin SDK + short-lived signed URLs).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
